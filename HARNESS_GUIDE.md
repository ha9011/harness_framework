클로드 코드(Claude Code)에게 프로젝트의 전체 컨텍스트를 한 번에 주입하고, 우리가 논의한 **'하네스 패턴'**대로 설계를 시작할 수 있도록 정리한 가이드입니다.

이 내용을 복사해서 `.md` 파일(예: `AGENT_SPEC.md`)로 저장하거나, 클로드 코드 터미널에 바로 붙여넣으시면 됩니다.

---

# 🏗️ 프로젝트 명세: AI 에이전트 하네스 프레임워크 (TS 기반)

## 1. 핵심 철학: "Agent = Model + Harness"
이 프로젝트는 모델의 추론 능력에 의존하는 것을 넘어, 모델을 엄격한 **상태 머신(State Machine)**과 **실행기(Executor)** 환경에 가두어 결과의 신뢰성을 보장하는 '하네스 공학'을 구현하는 것을 목표로 한다.

## 2. 자동 파이프라인 (Automated Pipeline)
에이전트는 아래의 5단계를 순차적으로 수행하며, **"순서 건너뛰기"**는 절대 불가하다.

| 단계 | 역할 | 비고 |
| :--- | :--- | :--- |
| **1. PLAN** | 작업 목표 수립 및 Todo 리스트 작성 | 직접 구현 금지, 전략만 수립 |
| **2. PRD** | 요구사항 상세화 및 관련 코드/문서 컨텍스트 분석 | LSP, AST-Grep 활용 권장 |
| **3. EXEC** | Executor를 통한 실제 코드 수정 및 명령 실행 | 시스템 직접 접근 대신 도구 호출 |
| **4. VERIFY** | 컴파일, 테스트, 린트 등을 통한 기계적 검증 | 성공 시 DONE, 실패 시 FIX로 이동 |
| **5. FIX** | VERIFY에서 발생한 에러 로그를 분석하여 원인 파악 | 분석 후 다시 EXEC로 회귀 |



## 3. 실행기 아키텍처 (Executor Pattern)
모든 도구는 `BaseExecutor` 추상 클래스를 상속받아 구현하며, TypeScript를 통해 입출력 규격을 엄격히 제한한다.

* **Type Safety**: `Zod`를 사용하여 AI가 던지는 인자(Arguments)를 런타임에서 검증한다.
* **Error Feedback**: 에러 발생 시 단순 실패 처리가 아니라, AI가 이해하고 스스로 수정할 수 있는 정제된 에러 메시지를 반환한다.
* **Encapsulation**: 모델은 시스템 내부 로직을 알 필요 없이 오직 규격화된 도구만 호출한다.

## 4. 기술 스택 및 구현 가이드
* **Runtime**: Node.js 또는 Bun (TypeScript 환경)
* **Pattern**: OOP 기반 추상 클래스 및 상태 머신(FSM)
* **Task**: "스스로 에러를 고치는 에이전트(Self-Healing Agent)" 루프 구현부터 시작

## 5. 클로드(AI)를 위한 지침 (System Instruction)
> "너는 이제부터 이 하네스 프레임워크 안에서 작동하는 에이전트야.
> 1. 모든 행동은 정의된 파이프라인 단계(`PLAN` -> `PRD` -> `EXEC` -> `VERIFY` -> `FIX`)를 따라야 해.
> 2. `EXEC` 단계에서 도구를 호출할 때는 반드시 제공된 `Executor` 규격을 준수해.
> 3. `VERIFY`에서 에러가 나면 좌절하지 말고, 그 에러 로그를 바탕으로 `FIX` 전략을 세운 뒤 다시 `EXEC`해.
> 4. 설명은 최소화하고, 행동의 결과와 상태 전이에 집중해."

---

