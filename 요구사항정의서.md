# 요구사항 정의서: 영어 패턴 학습기

## 1. 기능 요구사항

### FR-01. 단어 등록

| ID | 요구사항 | 우선순위 |
|----|---------|---------|
| FR-01-01 | 단어(word)와 뜻(meaning)을 직접 입력하여 단건 등록할 수 있다 | 필수 |
| FR-01-02 | JSON 형식으로 여러 단어를 한번에 벌크 등록할 수 있다 | 필수 |
| FR-01-03 | 벌크 등록 시 텍스트 영역에 JSON 플레이스홀더 예시가 표시된다 | 필수 |
| FR-01-04 | 이미지(유튜브 캡쳐 등)를 업로드하면 Gemini Vision으로 단어를 추출한다 | 필수 |
| FR-01-05 | 등록 시 Gemini가 품사/발음/유의어/한줄팁을 자동 보강한다 | 필수 |
| FR-01-06 | 벌크 등록은 1회 Gemini 호출로 일괄 보강한다 | 필수 |
| FR-01-07 | 보강 결과를 사용자가 확인/수정한 후 저장한다 | 필수 |
| FR-01-08 | 벌크 등록 시 보강 실패 항목은 보강 없이 저장한다 (부분 성공) | 필수 |
| FR-01-09 | 중복 단어는 알림 후 스킵한다 | 필수 |
| FR-01-10 | 단어에 중요 체크(⭐)를 토글할 수 있다 | 필수 |
| FR-01-11 | 단어를 soft delete할 수 있다 (예문은 유지, 해당 단어의 review_items만 soft delete) | 필수 |
| FR-01-12 | 단어 목록에서 검색(단어/뜻), 품사별 필터, 중요체크만 보기, 등록일 정렬이 가능하다 | 필수 |
| FR-01-13 | 단어 상세에서 해당 단어가 사용된 예문 목록을 볼 수 있다 | 필수 |
| FR-01-14 | 업로드된 이미지는 추출 후 저장하지 않는다 | 필수 |
| FR-01-15 | 등록 시 해당 날짜의 학습 기록에 자동 추가된다 | 필수 |

**JSON 벌크 등록 형식:**
```json
[
  { "word": "drink coffee", "meaning": "커피를 마시다" },
  { "word": "make a bed", "meaning": "침대를 정리하다" }
]
```

### FR-02. 패턴 등록

| ID | 요구사항 | 우선순위 |
|----|---------|---------|
| FR-02-01 | 패턴(template)과 한국어 설명(description)을 직접 입력하여 등록할 수 있다 | 필수 |
| FR-02-02 | 교재 사진을 업로드하면 Gemini Vision으로 패턴/설명/예문10개/해석을 추출한다 | 필수 |
| FR-02-03 | 추출 결과를 사용자가 확인/수정한 후 저장한다 | 필수 |
| FR-02-04 | 중복 패턴은 알림 후 스킵한다 | 필수 |
| FR-02-05 | 패턴을 soft delete할 수 있다 (예문은 유지, 해당 패턴의 review_items만 soft delete) | 필수 |
| FR-02-06 | 패턴 상세에서 교재 예문 목록 + 이 패턴으로 생성된 예문 목록을 볼 수 있다 | 필수 |
| FR-02-07 | 교재 예문의 순서가 보존된다 (1~10번) | 필수 |
| FR-02-08 | 업로드된 이미지는 추출 후 저장하지 않는다 | 필수 |
| FR-02-09 | 등록 시 해당 날짜의 학습 기록에 자동 추가된다 | 필수 |

### FR-03. 예문 생성

| ID | 요구사항 | 우선순위 |
|----|---------|---------|
| FR-03-01 | 난이도(유아/초등/중등/고등)를 선택할 수 있다 | 필수 |
| FR-03-02 | 생성 개수(10/20/30)를 선택할 수 있다 | 필수 |
| FR-03-03 | 단어 선택 우선순위: ⭐중요 체크 > 복습 횟수 적은 것 > 랜덤 | 필수 |
| FR-03-04 | 패턴은 등록된 것 중 랜덤 조합 | 필수 |
| FR-03-05 | Gemini API에 단어 목록(ID 포함)을 넘기고 JSON 응답으로 단어 매핑을 정확히 저장한다 | 필수 |
| FR-03-06 | 예문 1개에 패턴 1개 + 단어 N개가 매핑된다 | 필수 |
| FR-03-07 | 단어 상세에서 [예문 추가] 버튼으로 해당 단어 예문 N개를 생성할 수 있다 (패턴 없이) | 필수 |
| FR-03-11 | 패턴 상세에서 난이도+개수를 선택하여 해당 패턴으로 예문 N개를 생성할 수 있다 (단어는 우선순위 기반 자동 선택) | 필수 |
| FR-03-08 | 생성 이력을 조회할 수 있다 | 필수 |
| FR-03-09 | 예문마다 감정이입할 수 있는 상황(situation)을 5개씩 생성한다 | 필수 |
| FR-03-10 | 예문 표시 시 5개 상황 중 랜덤 1개를 보여준다 ("이 상황을 상상하며 읽어보세요") | 필수 |

### FR-04. 복습 (간격 반복)

| ID | 요구사항 | 우선순위 |
|----|---------|---------|
| FR-04-01 | 하루 복습 개수를 설정할 수 있다 (10/20/30, DB 저장) | 필수 |
| FR-04-02 | 오늘 복습할 카드를 자동 선정한다 (next_review_date <= 오늘) | 필수 |
| FR-04-03 | 선정 우선순위: ① 모름(HARD) 응답 ② 오래 안 본 것 ③ 복습 횟수 적은 것 | 필수 |
| FR-04-04 | 복습 화면은 [단어][패턴][문장] 탭으로 분리되어 원하는 타입만 골라서 복습할 수 있다. 각 탭에서 N개씩 독립 선정 (설정값 20이면 탭당 최대 20장. 부족한 타입은 있는 만큼만) | 필수 |
| FR-04-05 | 각 탭 내에서 랜덤 셔플하여 표시한다 | 필수 |
| FR-04-15 | 복습 완료 후 [처음부터 다시] 버튼으로 같은 카드를 읽기 전용으로 다시 볼 수 있다 (SM-2 미적용, 응답 버튼 없음) | 필수 |
| FR-04-16 | 복습 완료 후 [추가 복습] 버튼으로 이미 복습한 카드를 제외한 다음 N개를 가져올 수 있다. 더 이상 없으면 "복습할 카드가 없습니다" 표시 | 필수 |
| FR-04-06 | 카드는 양방향이다 (인식: 영→한 / 회상: 한→영) | 필수 |
| FR-04-07 | WORD 인식 카드: 앞면=영어 단어, 뒤면=뜻+예문(있으면)+팁 | 필수 |
| FR-04-08 | WORD 회상 카드: 앞면=한국어 뜻, 뒤면=영어 단어+발음+팁 | 필수 |
| FR-04-09 | PATTERN 인식 카드: 앞면=영어 패턴, 뒤면=한국어 설명+교재 예문 | 필수 |
| FR-04-10 | PATTERN 회상 카드: 앞면=한국어 설명, 뒤면=영어 패턴+교재 예문 | 필수 |
| FR-04-11 | SENTENCE 카드 (RECOGNITION만): 앞면=영어 예문, 뒤면=한국어 해석+상황 | 필수 |
| FR-04-12 | 기억남/애매/모름 응답 후 SM-2 알고리즘으로 간격 자동 조정 | 필수 |
| FR-04-13 | 단어/패턴/예문 등록 시 review_items 자동 생성 (WORD/PATTERN: 양방향, SENTENCE: RECOGNITION만) | 필수 |
| FR-04-14 | 동일 항목 중복 생성 방지 (UNIQUE 제약) | 필수 |

**SM-2 기반 커스텀 간격 조정 규칙:**
- 기억남(EASY): interval × ease_factor × 1.3, ease_factor += 0.15
- 애매(MEDIUM): interval × ease_factor, ease_factor 유지
- 모름(HARD): interval = 1 (리셋), ease_factor = max(1.3, ease_factor - 0.2)
- 초기값: interval_days = 1, ease_factor = 2.5

### FR-05. 학습 기록

| ID | 요구사항 | 우선순위 |
|----|---------|---------|
| FR-05-01 | 단어/패턴 등록 시 해당 날짜의 학습 기록이 자동 생성된다 | 필수 |
| FR-05-02 | 날짜별로 공부한 패턴 + 단어를 조회할 수 있다 | 필수 |
| FR-05-03 | day_number는 총 학습일 기준 (쉬는 날 상관없이 학습한 날만 카운트) | 필수 |

### FR-06. 홈 대시보드

| ID | 요구사항 | 우선순위 |
|----|---------|---------|
| FR-06-01 | 오늘의 복습 남은 개수 + [복습 시작] 버튼을 표시한다 | 필수 |
| FR-06-02 | 누적 통계 (단어 N개, 패턴 N개, 예문 N개, 복습 연속 N일)를 표시한다 | 필수 |
| FR-06-03 | 최근 학습 목록 (Day N - 패턴명)을 표시한다 | 필수 |

### FR-07. 설정

| ID | 요구사항 | 우선순위 |
|----|---------|---------|
| FR-07-01 | 하루 복습 개수를 변경할 수 있다 (10/20/30) | 필수 |
| FR-07-02 | 설정은 DB에 저장되어 어디서든 동일하게 적용된다 | 필수 |

---

## 2. 비기능 요구사항

| ID | 요구사항 | 설명 |
|----|---------|------|
| NFR-01 | soft delete | 단어/패턴/예문은 물리 삭제 대신 deleted 플래그 사용 |
| NFR-02 | 삭제 연쇄 | 단어/패턴 삭제 시 예문은 유지, 해당 항목의 review_items만 soft delete |
| NFR-03 | TDD | 테스트를 먼저 작성하고 구현 (서비스, 컨트롤러) |
| NFR-04 | 이미지 미저장 | 업로드 이미지는 AI 추출 후 저장하지 않음 |
| NFR-05 | 중복 방지 | words.word UNIQUE, patterns.template UNIQUE |

---

## 3. 데이터 모델

### 테이블 목록
| 테이블 | 설명 |
|--------|------|
| words | 단어/표현 (AI 보강 정보 포함) |
| patterns | 영어 패턴 |
| pattern_examples | 패턴 등록 시 교재 예문 (순서 보존) |
| generation_history | 예문 생성 이력 (요청 단위) |
| generated_sentences | AI 생성 예문 |
| generated_sentence_words | 예문-단어 매핑 (N:1) |
| sentence_situations | 예문별 상황 5개 (감정이입용, 랜덤 1개 표시) |
| study_records | 날짜별 학습 기록 |
| study_record_items | 학습 기록 상세 (패턴+단어) |
| review_items | 복습 대상 (간격 반복 상태) |
| review_logs | 복습 이력 |
| user_settings | 사용자 설정 |

### words
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | BIGSERIAL PK | |
| word | VARCHAR(200) UNIQUE | 단어 또는 표현 — 사용자 입력 |
| meaning | VARCHAR(500) | 한국어 뜻 — 사용자 입력 |
| part_of_speech | VARCHAR(50) | 품사 — AI 보강 |
| pronunciation | VARCHAR(200) | 발음 — AI 보강 |
| synonyms | VARCHAR(500) | 유의어/관련 표현 — AI 보강 |
| tip | VARCHAR(500) | 한줄 팁 — AI 보강 |
| is_important | BOOLEAN DEFAULT false | 중요 체크 |
| deleted | BOOLEAN DEFAULT false | soft delete |
| created_at | TIMESTAMP | |
| updated_at | TIMESTAMP | |

### patterns
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | BIGSERIAL PK | |
| template | VARCHAR(255) UNIQUE | 패턴 템플릿 |
| description | VARCHAR(500) | 한국어 설명 |
| deleted | BOOLEAN DEFAULT false | soft delete |
| created_at | TIMESTAMP | |
| updated_at | TIMESTAMP | |

### pattern_examples
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | BIGSERIAL PK | |
| pattern_id | BIGINT FK | patterns.id |
| sort_order | INT | 예문 순서 (1~10) |
| sentence | VARCHAR(500) | 영어 예문 |
| translation | VARCHAR(500) | 한국어 해석 |

### generation_history (예문 생성 이력)
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | BIGSERIAL PK | |
| level | VARCHAR(10) | 유아/초등/중등/고등 |
| requested_count | INT | 요청한 생성 개수 |
| actual_count | INT | 실제 생성된 개수 |
| word_id | BIGINT FK nullable | 단어 지정 생성 시 |
| pattern_id | BIGINT FK nullable | 패턴 지정 생성 시 |
| created_at | TIMESTAMP | |

### generated_sentences
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | BIGSERIAL PK | |
| generation_id | BIGINT FK | generation_history.id |
| pattern_id | BIGINT FK nullable | 사용된 패턴 (단어만 생성 시 null) |
| sentence | TEXT | 생성된 영어 예문 |
| translation | TEXT | 한국어 해석 |
| level | VARCHAR(10) | 유아/초등/중등/고등 |
| deleted | BOOLEAN DEFAULT false | soft delete |
| created_at | TIMESTAMP | |

### generated_sentence_words
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | BIGSERIAL PK | |
| sentence_id | BIGINT FK | generated_sentences.id |
| word_id | BIGINT FK | words.id |

### sentence_situations (예문별 상황 — 감정이입용)
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | BIGSERIAL PK | |
| sentence_id | BIGINT FK | generated_sentences.id |
| situation | TEXT | 상황 설명 (예: "택시 안에서 친구에게 전화하는 상황") |

### study_records
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | BIGSERIAL PK | |
| study_date | DATE UNIQUE | 학습 날짜 (하루에 하나) |
| day_number | INT | 총 학습일 기준 몇 일차 |
| created_at | TIMESTAMP | |

### study_record_items
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | BIGSERIAL PK | |
| study_record_id | BIGINT FK | study_records.id |
| item_type | VARCHAR(10) | WORD / PATTERN |
| item_id | BIGINT | 해당 단어 또는 패턴의 ID |
| UNIQUE(study_record_id, item_type, item_id) | | 같은 날 같은 항목 중복 방지 |

### review_items
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | BIGSERIAL PK | |
| item_type | VARCHAR(20) | WORD / PATTERN / SENTENCE |
| item_id | BIGINT | 해당 타입의 ID |
| direction | VARCHAR(15) DEFAULT 'RECOGNITION' | RECOGNITION(영→한) / RECALL(한→영). SENTENCE는 RECOGNITION만 |
| deleted | BOOLEAN DEFAULT false | soft delete 연쇄용 |
| next_review_date | DATE | 다음 복습 날짜 |
| interval_days | INT DEFAULT 1 | 현재 간격 |
| ease_factor | FLOAT DEFAULT 2.5 | 난이도 계수 |
| review_count | INT DEFAULT 0 | 복습 횟수 |
| last_result | VARCHAR(10) | 마지막 응답 (EASY/MEDIUM/HARD) |
| last_reviewed_at | TIMESTAMP | |
| created_at | TIMESTAMP | |
| UNIQUE(item_type, item_id, direction) | | 중복 방지 |

### review_logs
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | BIGSERIAL PK | |
| review_item_id | BIGINT FK | review_items.id |
| result | VARCHAR(10) | EASY / MEDIUM / HARD |
| reviewed_at | TIMESTAMP | |

### user_settings
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | BIGSERIAL PK | |
| setting_key | VARCHAR(50) UNIQUE | 설정 키 |
| setting_value | VARCHAR(200) | 설정 값 |
| updated_at | TIMESTAMP | |
