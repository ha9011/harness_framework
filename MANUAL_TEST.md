# 수동 테스트 케이스

> 순서대로 진행. 각 항목 체크(✅/❌) 후 비고 기록.

## 사전 조건

- [ ] Docker Desktop 실행 중
- [ ] `docker compose up -d` → PostgreSQL 기동 확인
- [ ] `cd backend && ./gradlew bootRun --args='--spring.profiles.active=local'` → 8080 기동
- [ ] `cd frontend && npm run dev` → 3000 기동
- [ ] 브라우저에서 http://localhost:3000 접속

---

## 1단계: 홈 대시보드 (빈 상태)

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 1.1 | 최초 접속 | 커피나무(씨앗) + "아직 등록된 학습 데이터가 없어요" + "단어 등록하러 가기" 링크 | |
| 1.2 | 누적 통계 | 단어 0, 패턴 0, 예문 0, 연속일 0 | |
| 1.3 | 하단 네비게이션 | 5개 탭 표시, 홈 활성(색상 다름) | |
| 1.4 | "단어 등록하러 가기" 클릭 | /words 페이지 이동 | |

---

## 2단계: 단어 등록

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 2.1 | 단어 목록 페이지 진입 | 빈 상태 표시 | |
| 2.2 | 하단 네비게이션 "단어" 탭 활성 확인 | 📖 단어가 primary 색상 | |
| 2.3 | 추가 버튼 클릭 | 모달 올라옴 | |
| 2.4 | 모달 배경 클릭 | 모달 닫힘 | |
| 2.5 | word만 입력, meaning 비움 → 등록 시도 | 등록 안 됨 (빈 필드 방어) | |
| 2.6 | 단건 등록: word="apple", meaning="사과" | 등록 완료 토스트 → 목록에 표시 | |
| 2.7 | 같은 단어 재등록 | 409 에러 → "이미 등록된 단어" 메시지 | |
| 2.8 | 벌크 탭 전환 → 빈 JSON 등록 `[]` | 400 에러 → 에러 메시지 표시 | |
| 2.9 | 벌크 등록: `[{"word":"banana","meaning":"바나나"},{"word":"cherry","meaning":"체리"}]` | saved: 2 표시 | |
| 2.10 | 벌크에 중복 포함: `[{"word":"apple","meaning":"사과"},{"word":"dog","meaning":"개"}]` | saved: 1, skipped: 1 | |
| 2.11 | 목록에서 4개 단어 확인 | apple, banana, cherry, dog 카드 표시 | |
| 2.12 | 검색: "app" 입력 | apple만 표시 | |
| 2.13 | 검색 지우기 | 전체 복귀 | |
| 2.14 | ⭐ 중요 토글 (apple) | 별 활성화 | |
| 2.15 | "중요만" 필터 | apple만 표시 | |
| 2.16 | 단어 상세 (apple 클릭) | 상세 페이지: AI 보강 정보(품사, 발음, 유의어, 팁) 표시 | |
| 2.17 | 이미지 추출 (단어 목록 사진 업로드) | Gemini Vision → 단어 목록 미리보기 | |

---

## 3단계: 패턴 등록

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 3.1 | 패턴 탭 이동 | 빈 상태 | |
| 3.2 | 추가 버튼 → 직접 등록 | 모달 표시 | |
| 3.3 | 패턴 등록: template="I want to ~", description="~하고 싶다", 예문 2~3개 | 등록 성공 | |
| 3.4 | 같은 패턴 재등록 | 409 중복 에러 | |
| 3.5 | 패턴 상세 진입 | 패턴 정보 + 교재 예문 순서대로 표시 | |
| 3.6 | 이미지 추출 (교재 사진 업로드) | Gemini Vision 호출 → 추출 결과 미리보기 | |

---

## 4단계: 예문 생성

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 4.1 | 생성 탭 이동 | 난이도/개수 선택 UI | |
| 4.2 | 난이도: 초등, 개수: 10 → 생성 | 로딩 후 예문 카드 10개 표시 | |
| 4.3 | 예문 카드 확인 | 영어 문장 + 상황 말풍선(SituationCloud) | |
| 4.4 | 해석 펼침 (카드 탭/클릭) | 한국어 번역 표시 | |
| 4.5 | 생성 이력 탭 | 방금 생성한 이력 1건 표시 (초등, 요청10개) | |
| 4.6 | 단어 0개 상태에서 생성 시도 (해당 없음 - 이미 등록함) | — | |

---

## 5단계: 복습

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 5.1 | 복습 탭 이동 | [단어][패턴][문장] 탭 + 남은 카드 수 뱃지 | |
| 5.2 | 단어 탭 선택 | 카드 표시 (앞면: 영어 단어) | |
| 5.3 | 카드 탭/클릭 | **플립 애니메이션 (0.4초 회전)** → 뒷면 표시 (뜻 + 품사 + 예문) | |
| 5.4 | WORD RECOGNITION 카드 뒷면 | 예문 최대 3개 표시 (4개 이상이면 랜덤 3개) | |
| 5.5 | EASY 버튼 | 다음 카드로 이동 | |
| 5.6 | MEDIUM 버튼 | 다음 카드로 이동 | |
| 5.7 | HARD 버튼 | 다음 카드로 이동 | |
| 5.8 | 모든 카드 완료 | 완료 화면 표시 | |
| 5.9 | "처음부터 다시" 클릭 | 같은 카드 재표시 (읽기 전용, EASY/MEDIUM/HARD 버튼 없음) | |
| 5.10 | "추가 복습" 클릭 | 이미 한 카드 제외, 남은 카드 요청 (없으면 "복습 완료" 메시지) | |
| 5.11 | 패턴 탭 전환 | 패턴 카드 표시 (RECOGNITION: 앞=템플릿, 뒤=설명+예문) | |
| 5.12 | 문장 탭 전환 | 문장 카드 (앞=영어+상황 말풍선, 뒤=한국어 번역) | |
| 5.13 | 문장 카드 상황 확인 | 같은 문장이라도 새로고침 시 상황이 바뀔 수 있음 (5개 중 랜덤) | |
| 5.14 | 모든 타입 복습 완료 후 복습 탭 재진입 | 세 탭 모두 0장, 빈 상태 | |

---

## 6단계: 대시보드 (데이터 있는 상태)

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 6.1 | 홈으로 이동 | 빈 상태 메시지 사라짐 | |
| 6.2 | 누적 통계 | 단어 4, 패턴 1, 예문 10, 연속일 1 (복습 했으므로) | |
| 6.3 | 커피나무 | 새싹 표시 (streak=1) | |
| 6.4 | 오늘의 복습 | 타입별 남은 개수 표시 (5단계에서 전부 했으면 0) | |
| 6.5 | 복습 남은 게 있으면 "오늘의 복습 시작" 버튼 표시 | /review 이동 | |
| 6.6 | 최근 학습 | Day 1 기록 (단어 4개, 패턴 1개) | |
| 6.7 | "모두 보기" 링크 | /history 이동 | |

---

## 7단계: 학습 기록 + 설정

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 7.1 | 홈 → "모두 보기" 클릭 | /history 이동, Day 1 기록 표시 | |
| 7.2 | 날짜 형식 | "2026. 5. 5." 형식 (ISO 아님) | |
| 7.3 | 홈 → "설정" 클릭 | /settings 이동 | |
| 7.4 | 현재 값 | 10개 선택됨 (기본값) | |
| 7.5 | 20개 선택 → 저장 | "저장 완료" 표시 | |
| 7.6 | 새로고침 후 설정 확인 | 20개 유지 | |
| 7.7 | 백엔드 중단 후 저장 시도 | "저장에 실패했습니다" 에러 메시지 | |

---

## 8단계: 삭제 + 엣지케이스

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 8.1 | 단어 상세 → 삭제 (apple) | 목록에서 사라짐 | |
| 8.2 | 복습 탭 → 단어 탭 | apple 카드 안 나옴 (WORD review_items 삭제됨) | |
| 8.3 | 복습 탭 → 문장 탭 | apple 포함 예문 카드는 **여전히 나옴** (SENTENCE 유지) | |
| 8.4 | 패턴 삭제 | 목록에서 사라짐, PATTERN 복습 카드 안 나옴 | |
| 8.5 | 홈 대시보드 | 단어 2 (banana, cherry), 패턴 0 으로 업데이트 | |

---

## 9단계: Gemini 연동 확인

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 9.1 | 단어 등록 시 AI 보강 | 품사, 발음, 유의어, 팁이 자동 채워짐 | |
| 9.2 | AI 보강 실패 (잘못된 API 키) | 보강 없이 저장됨 (word+meaning만) | |
| 9.3 | 이미지 추출 성공 | 사진에서 단어/패턴 추출 결과 표시 | |
| 9.4 | 예문 생성 시 상황 5개 | 상세 보면 situation이 매번 랜덤 1개 다르게 나옴 | |

---

## 10단계: SM-2 간격 검증 (선택 — 다음 날 테스트)

> 이 단계는 하루 뒤에 진행해야 의미 있음. 당일 테스트 불가.

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 10.1 | 다음 날 접속 → 복습 탭 | HARD 누른 카드만 다시 나옴 (interval=1) | |
| 10.2 | EASY 누른 카드 | 안 나옴 (interval=3일 이상) | |
| 10.3 | streak | 어제+오늘 복습했으면 연속일 2 | |
| 10.4 | 커피나무 | 새싹 유지 (streak=2) | |

---

## 비고

- 테스트 순서가 중요: 2단계에서 등록한 데이터를 이후 단계에서 사용
- 2단계에서 벌크 중복 포함 테스트(2.10)로 단어가 4개(apple, banana, cherry, dog)가 됨
- 5단계(복습)는 등록 직후 next_review_date=오늘이므로 바로 테스트 가능
- 8단계 삭제 후에는 데이터가 줄어들므로 마지막에 진행
- 10단계(SM-2 간격)는 다음 날 진행

---
---

# Phase 1-auth: 회원가입/로그인 + 데이터 격리 테스트

> Phase 1을 테스트하려면 DB를 초기화해야 한다: `docker compose down -v && docker compose up -d`
> 기존 MVP 데이터에 user_id가 없어서 충돌할 수 있으므로 반드시 DB를 새로 시작하라.

## 사전 조건

- [ ] `docker compose down -v && docker compose up -d` (DB 초기화)
- [ ] `cd backend && ./gradlew bootRun` → 8080 기동 (users 테이블 자동 생성 확인)
- [ ] `cd frontend && npm run dev` → 3000 기동
- [ ] 브라우저 2개 준비 (일반 + 시크릿 모드) — 데이터 격리 테스트용

---

## 11단계: 회원가입

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 11.1 | http://localhost:3000 접속 | **/login 페이지로 리다이렉트** (홈 안 보임) | |
| 11.2 | /login 페이지 확인 | 창가 일러스트 SVG + "같은 자리, 같은 잔으로 다시 시작해요" 카피 | |
| 11.3 | /login에서 BottomNav 확인 | **하단 네비게이션 없음** | |
| 11.4 | "회원가입 →" 링크 클릭 | /signup 페이지 이동 | |
| 11.5 | /signup 페이지 확인 | TopBar "회원가입" + "처음 오신 걸 환영해요" 카피 | |
| 11.6 | /signup에서 BottomNav 확인 | **하단 네비게이션 없음** | |
| 11.7 | 비밀번호 `short` 입력 → 다른 필드로 포커스 이동 | "8글자 이상" 에러 메시지 (onBlur 시점) | |
| 11.8 | 비밀번호 확인 필드 비어있는 상태 | 에러 미표시 | |
| 11.9 | 비밀번호: `password123`, 확인: `different1` → 포커스 이동 | "비밀번호가 일치하지 않습니다" | |
| 11.10 | 비밀번호: `password123`, 확인: `password123` | **세이지 체크 뱃지** 표시 | |
| 11.11 | 닉네임: `카페주인`, 이메일: `owner@cafe.com`, 비밀번호: `password123`, 확인: `password123` → "가입하기" | 홈 이동 + "**카페주인님 안녕하세요**" | |
| 11.12 | 같은 이메일로 다시 가입 시도 (다른 브라우저에서) | "이미 등록된 이메일입니다" 에러 (409) | |

---

## 12단계: 로그인 + 로그아웃

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 12.1 | 로그아웃 (11단계 이후) | /login으로 이동 | |
| 12.2 | 개발자 도구 → Application → Cookies | "token" Cookie **없음** | |
| 12.3 | 뒤로가기 | /login 유지 (홈으로 못 돌아감) | |
| 12.4 | 이메일: `owner@cafe.com`, 비밀번호: `password123` → 로그인 | 홈 이동 + "카페주인님 안녕하세요" | |
| 12.5 | 잘못된 비밀번호: `wrong` | "이메일 또는 비밀번호가 올바르지 않습니다" | |
| 12.6 | 존재하지 않는 이메일: `nobody@x.com` | **동일 메시지** (이메일 존재 여부 노출 안 함) | |
| 12.7 | F5 새로고침 | 홈 유지 (인증 상태 유지, 깜빡임 없음) | |

---

## 13단계: 라우트 보호 (미로그인 차단)

> 로그아웃 상태에서 직접 URL 입력

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 13.1 | http://localhost:3000 | /login 리다이렉트 | |
| 13.2 | http://localhost:3000/words | /login 리다이렉트 | |
| 13.3 | http://localhost:3000/patterns | /login 리다이렉트 | |
| 13.4 | http://localhost:3000/generate | /login 리다이렉트 | |
| 13.5 | http://localhost:3000/review | /login 리다이렉트 | |
| 13.6 | http://localhost:3000/history | /login 리다이렉트 | |
| 13.7 | http://localhost:3000/settings | /login 리다이렉트 | |

---

## 14단계: 사용자별 데이터 격리

> **브라우저 A** = 일반 모드, **브라우저 B** = 시크릿 모드

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 14.1 | **A**: `userA@test.com` / `password123` / `사용자A` 로 가입 | 홈 이동 | |
| 14.2 | **A**: 단어 "apple" / "사과" 등록 | 목록에 표시 | |
| 14.3 | **A**: 단어 "banana" / "바나나" 등록 | 목록에 2개 | |
| 14.4 | **A**: 패턴 "I want to ~" / "~하고 싶다" 등록 | 패턴 목록 1개 | |
| 14.5 | **B**: `userB@test.com` / `password123` / `사용자B` 로 가입 | 홈 이동 | |
| 14.6 | **B**: 단어 목록 확인 | **비어있음** (A의 단어 안 보임) | |
| 14.7 | **B**: 패턴 목록 확인 | **비어있음** (A의 패턴 안 보임) | |
| 14.8 | **B**: 홈 대시보드 확인 | 단어 0, 패턴 0, 예문 0 | |
| 14.9 | **B**: 단어 "apple" / "사과" 등록 | **성공** (A와 같은 단어 독립 등록) | |
| 14.10 | **B**: 복습 페이지 → 단어 탭 | B의 "apple" 카드만 표시 (A의 apple/banana 안 보임) | |
| 14.11 | **A**: 단어 목록 새로고침 | 여전히 apple, banana 2개만 (B의 apple 안 보임) | |
| 14.12 | **A**: "apple" 삭제 | 목록에서 사라짐, banana만 남음 | |
| 14.13 | **B**: 단어 목록 확인 | B의 "apple" **여전히 존재** (A 삭제에 영향 없음) | |
| 14.14 | **A**: 대시보드 확인 | 단어 1 (banana만), 패턴 1 | |
| 14.15 | **B**: 대시보드 확인 | 단어 1 (apple만), 패턴 0 | |

---

## 15단계: IDOR 방어 (다른 사용자 리소스 직접 접근)

> 개발자 도구 Console 사용

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 15.1 | **A**: Network 탭에서 단어 등록 응답 → ID 확인 (예: id=3) | - | |
| 15.2 | **B**: Console에서 `fetch('http://localhost:8080/api/words/3', {credentials:'include'}).then(r=>console.log(r.status))` | **403 또는 404** | |
| 15.3 | **B**: `fetch('http://localhost:8080/api/words/3', {method:'DELETE', credentials:'include'}).then(r=>console.log(r.status))` | **403 또는 404** | |
| 15.4 | **A**: 복습 시작 → Network에서 reviewItemId 확인 (예: id=7) | - | |
| 15.5 | **B**: `fetch('http://localhost:8080/api/reviews/7', {method:'POST', headers:{'Content-Type':'application/json'}, body:'{"result":"EASY"}', credentials:'include'}).then(r=>console.log(r.status))` | **403 또는 404** | |
| 15.6 | 미인증 상태에서 `fetch('http://localhost:8080/api/words').then(r=>console.log(r.status))` | **401** | |

---

## 16단계: JWT 만료 시뮬레이션

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 16.1 | 로그인 상태 확인 | 홈 정상 표시 | |
| 16.2 | 개발자 도구 → Application → Cookies → "token" 삭제 | - | |
| 16.3 | 페이지에서 아무 동작 (단어 등록, 탭 이동 등) | **/login으로 자동 리다이렉트** | |

---

## 17단계: 로그아웃 → 다른 계정 로그인 (데이터 전환)

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 17.1 | 사용자 A 로그인 → 단어 목록 확인 | A의 단어 표시 | |
| 17.2 | 로그아웃 | /login 이동 | |
| 17.3 | 사용자 B 로그인 | 홈 표시 "사용자B님 안녕하세요" | |
| 17.4 | 단어 목록 확인 | **B의 데이터만** (A 데이터 잔존 없음, 깜빡임 없음) | |
| 17.5 | 대시보드 확인 | B의 카운트만 | |

---

## 18단계: UI/UX 확인

| # | 확인 항목 | 기대 | 통과 |
|---|----------|------|------|
| 18.1 | 로그인 입력 필드 스타일 | `bg-raised rounded-[14px]` (기존 페이지의 bg-soft와 **의도적으로 다름**) | |
| 18.2 | 로그인 입력 아이콘 | 이메일: 메일 아이콘, 비밀번호: 자물쇠 아이콘 | |
| 18.3 | 비밀번호 눈 토글 | 클릭 시 비밀번호 표시/숨김 전환 | |
| 18.4 | 홈 인사 | "{닉네임}님 안녕하세요" + "오늘도 한 모금 천천히" | |
| 18.5 | 로그인 후 BottomNav | 5개 탭 정상 표시 | |
| 18.6 | FlipCard 앞면 | bg-raised (크림) + 강화 shadow | |
| 18.7 | FlipCard 뒷면 (탭하여 뒤집기) | **bg-primary-soft** (#E8D5BE, 라떼 소프트 톤) | |

---

## 19단계: 기존 MVP 기능 정상 동작 (회귀 테스트)

> 인증 추가 후 기존 기능이 깨지지 않았는지 확인

| # | 테스트 | 예상 결과 | 통과 |
|---|--------|-----------|------|
| 19.1 | 단어 단건 등록 + AI 보강 | 품사/발음/유의어/팁 표시 (GEMINI_API_KEY 필요) | |
| 19.2 | 단어 벌크 등록 | saved/skipped/enrichmentFailed 분리 응답 | |
| 19.3 | 단어 중복 등록 | 409 에러 | |
| 19.4 | 단어 ⭐ 중요 토글 | 토글 동작 | |
| 19.5 | 단어 검색 + 필터 | 정상 동작 | |
| 19.6 | 패턴 등록 (직접 입력) | 성공 | |
| 19.7 | 예문 생성 (난이도 + 개수) | 예문 카드 표시 | |
| 19.8 | 복습 SM-2 (EASY/MEDIUM/HARD) | 카드 넘김 + 완료 화면 | |
| 19.9 | "처음부터 다시" | 읽기 전용 모드 | |
| 19.10 | 설정 변경 (복습 개수) | 저장 + 유지 | |
| 19.11 | 대시보드 streak | 복습 후 연속일 1 표시 | |
| 19.12 | 학습 기록 | Day N 기록 표시 | |
| 19.13 | 삭제 연쇄 (단어 삭제 → WORD 복습카드 삭제, SENTENCE 유지) | 정상 동작 | |

---

## Phase 1 테스트 완료 체크리스트

### 11단계: 회원가입
- [ ] 11.1 최초 접속 → /login 리다이렉트
- [ ] 11.2 로그인 페이지 일러스트 + 카피
- [ ] 11.3 /login BottomNav 없음
- [ ] 11.4 "회원가입 →" 링크 → /signup 이동
- [ ] 11.5 /signup TopBar + 카피
- [ ] 11.6 /signup BottomNav 없음
- [ ] 11.7 비밀번호 8글자 미만 에러 (onBlur)
- [ ] 11.8 비밀번호 확인 비어있으면 에러 없음
- [ ] 11.9 비밀번호 불일치 에러
- [ ] 11.10 비밀번호 일치 → 세이지 체크 뱃지
- [ ] 11.11 정상 가입 → 홈 이동 + 닉네임 표시
- [ ] 11.12 이메일 중복 에러 (409)

### 12단계: 로그인/로그아웃
- [ ] 12.1 로그아웃 → /login 이동
- [ ] 12.2 Cookie "token" 삭제 확인
- [ ] 12.3 뒤로가기 → /login 유지
- [ ] 12.4 정상 로그인 → 홈 + 닉네임
- [ ] 12.5 잘못된 비밀번호 → 통일 에러 메시지
- [ ] 12.6 존재하지 않는 이메일 → 동일 메시지
- [ ] 12.7 F5 새로고침 → 인증 유지 (깜빡임 없음)

### 13단계: 라우트 보호
- [ ] 13.1 / → /login
- [ ] 13.2 /words → /login
- [ ] 13.3 /patterns → /login
- [ ] 13.4 /generate → /login
- [ ] 13.5 /review → /login
- [ ] 13.6 /history → /login
- [ ] 13.7 /settings → /login

### 14단계: 데이터 격리
- [ ] 14.1 A 가입+로그인
- [ ] 14.2 A 단어 "apple" 등록
- [ ] 14.3 A 단어 "banana" 등록
- [ ] 14.4 A 패턴 "I want to ~" 등록
- [ ] 14.5 B 가입+로그인
- [ ] 14.6 B 단어 목록 비어있음
- [ ] 14.7 B 패턴 목록 비어있음
- [ ] 14.8 B 대시보드 0/0/0
- [ ] 14.9 B "apple" 독립 등록 성공
- [ ] 14.10 B 복습 → B 카드만
- [ ] 14.11 A 목록 → A 데이터만
- [ ] 14.12 A "apple" 삭제
- [ ] 14.13 B "apple" 여전히 존재
- [ ] 14.14 A 대시보드 → 단어 1
- [ ] 14.15 B 대시보드 → 단어 1

### 15단계: IDOR 방어
- [ ] 15.1 A 단어 ID 확인
- [ ] 15.2 B가 A 단어 조회 → 403/404
- [ ] 15.3 B가 A 단어 삭제 → 403/404
- [ ] 15.4 A 복습 카드 ID 확인
- [ ] 15.5 B가 A 복습 카드 조작 → 403/404
- [ ] 15.6 미인증 API 호출 → 401

### 16단계: JWT 만료
- [ ] 16.1 로그인 상태 확인
- [ ] 16.2 Cookie "token" 수동 삭제
- [ ] 16.3 동작 시도 → /login 자동 리다이렉트

### 17단계: 계정 전환
- [ ] 17.1 A 로그인 → 단어 확인
- [ ] 17.2 로그아웃
- [ ] 17.3 B 로그인 → "사용자B님 안녕하세요"
- [ ] 17.4 단어 → B 데이터만 (A 잔존 없음)
- [ ] 17.5 대시보드 → B 카운트만

### 18단계: UI/UX
- [ ] 18.1 로그인 입력 필드 bg-raised rounded-14
- [ ] 18.2 입력 아이콘 (메일/자물쇠)
- [ ] 18.3 비밀번호 눈 토글
- [ ] 18.4 홈 "{닉네임}님 안녕하세요" + 부제
- [ ] 18.5 로그인 후 BottomNav 정상 표시
- [ ] 18.6 FlipCard 앞면 shadow 강화
- [ ] 18.7 FlipCard 뒷면 bg-primary-soft

### 19단계: 기존 MVP 회귀
- [ ] 19.1 단어 단건 등록 + AI 보강
- [ ] 19.2 단어 벌크 등록
- [ ] 19.3 단어 중복 409
- [ ] 19.4 단어 ⭐ 중요 토글
- [ ] 19.5 단어 검색 + 필터
- [ ] 19.6 패턴 등록
- [ ] 19.7 예문 생성
- [ ] 19.8 복습 SM-2
- [ ] 19.9 "처음부터 다시" 읽기 전용
- [ ] 19.10 설정 변경
- [ ] 19.11 대시보드 streak
- [ ] 19.12 학습 기록
- [ ] 19.13 삭제 연쇄
